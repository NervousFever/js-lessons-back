// Hàm này hỗ trợ gán req[key] default là functionCode để các phiên sau sử dụng tra cứu
module.exports = (value, key = "functionCode") => {
    return (req, res, next) => {
        req[key] = value;
        next();
    }
}