const {
  expHandlers,
  validators,
  arrObj,
  dateUtil,
  excelUtils,
  expUtil,
  htmlUtil,
  ipInfo,
  jsEncrypt,
  ldapLogin,
  myDevice,
  pdfUtil,
  postHandler,
  qrCodeUtil,
  rsaService,
  secretUtil,
  uniqueid,
  md5,
  vnHandler,
  waiting,
  webService,
  createSubFolder,
  convertPath2ObjectName,
  createEntryIndex
} = require("../index");

console.log("This time:", arrObj.getTimestamp());

createEntryIndex(__dirname,true);

console.log("Md5:", md5("abc"));

console.log("uniqueid:", uniqueid.createShortId());
